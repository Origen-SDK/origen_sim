database -open waves -into <%= options[:dir] %> -default -event
probe -create -shm origen_tb -depth all -database waves
probe -create -assertions -transaction origen_tb -depth all -database waves
run
exit
